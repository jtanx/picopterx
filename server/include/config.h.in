/**
 * @file config.h
 * @brief System-specific and compile-time specific configuration parameters.
 */
 
#ifndef _CONFIG_H
#define _CONFIG_H 

#define PICOPTER_VERSION ${PICOPTER_VERSION}
#define PICOPTER_DATE "${PICOPTER_DATE}"

/* Do we use syslog? */
#cmakedefine USE_SYSLOG


#endif // _CONFIG_H